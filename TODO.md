# TODO

- Support the expanded-rate-limit API key.
- add support for the undocumented tourney and playoffs endpoints
- Clarify docs for fields like tag, type, etc, and update typechecking too.

## Later

- Design an optional custom retry-delay function.
- Consider caller-provided response schemas for custom `request()` paths.
